export type Side = 'YES' | 'NO';
export type MarketType = 'deterministic' | 'structured' | 'judgment';
export interface Source { name:string; url:string; jsonPath?:string|null; timestampPath?:string|null; timestampValue?:string|null; fetchedAt?:string|null; contentHash?:string|null }
export interface Market { id:string; title:string; category:string; type:MarketType; status:string; creator?:string; outcome?:Side|'VOID'|null; preliminaryOutcome?:Side|'VOID'|null; probability:number; volume:number; liquidity:number; closesAt:string; feeBps:number; sources:Source[]; resolutionSpec:{summary:string;criteria:string[];observationTime?:string;resolver?:{contract:string;args:Record<string,unknown>}}; resolutionSpecHash?:string; sourcesHash?:string; resolverContractAddress?:string|null; marketContractAddress?:string|null; marketDeploymentTx?:string|null; settlement?:'onchain'|'simulation'; challengeWindowClosesAt?:string|null; priceSeries?:Array<{t:string;p:number}>; traderCount?:number; poolDepth?:{yesReserve:number;noReserve:number;collateral:number;fees:number}; recentTrades?:Array<{id:string;type:'buy'|'sell';side:Side;shares:number;price:number;total:number;fee:number;timestamp:string;trader:string}> }
export interface Quote { action:'buy'|'sell'; side:Side; amount?:number; shares:number; amountOut?:number; fee:number; avgPrice:number; spotPrice:number; priceImpact:number }
export interface Position { id:string;marketId:string;side:Side;shares:number;avgPrice:number;currentPrice:number;status:string;totalCost:number }
export interface Portfolio { address:string;totalValue:number;totalPnl:number;positions:Position[];liquidityPositions?:Array<{marketId:string;shares:number;status:string;claimedAmount?:number}>;history:Array<{id:string;type:string;side:Side;total:number;timestamp:string}> }
